/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SearchResponseDto } from '../../models/search-response-dto';

export interface SearchControllerSearch$Params {

/**
 * Search query string
 */
  q: string;

/**
 * Comma-separated categories to search (nodes,devices,alerts,projects,owners)
 */
  categories?: Array<string>;

/**
 * Max results per category (1-20)
 */
  limit?: number;
}

export function searchControllerSearch(http: HttpClient, rootUrl: string, params: SearchControllerSearch$Params, context?: HttpContext): Observable<StrictHttpResponse<SearchResponseDto>> {
  const rb = new RequestBuilder(rootUrl, searchControllerSearch.PATH, 'get');
  if (params) {
    rb.query('q', params.q, {});
    rb.query('categories', params.categories, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SearchResponseDto>;
    })
  );
}

searchControllerSearch.PATH = '/api/search';
