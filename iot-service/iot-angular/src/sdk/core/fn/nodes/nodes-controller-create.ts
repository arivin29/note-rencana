/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateNodeDto } from '../../models/create-node-dto';
import { NodeResponseDto } from '../../models/node-response-dto';

export interface NodesControllerCreate$Params {
      body: CreateNodeDto
}

export function nodesControllerCreate(http: HttpClient, rootUrl: string, params: NodesControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<NodeResponseDto>> {
  const rb = new RequestBuilder(rootUrl, nodesControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NodeResponseDto>;
    })
  );
}

nodesControllerCreate.PATH = '/api/nodes';
