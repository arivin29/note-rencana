/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NodeResponseDto } from '../../models/node-response-dto';
import { UpdateNodeDto } from '../../models/update-node-dto';

export interface NodesControllerUpdate$Params {
  id: string;
      body: UpdateNodeDto
}

export function nodesControllerUpdate(http: HttpClient, rootUrl: string, params: NodesControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<NodeResponseDto>> {
  const rb = new RequestBuilder(rootUrl, nodesControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NodeResponseDto>;
    })
  );
}

nodesControllerUpdate.PATH = '/api/nodes/{id}';
