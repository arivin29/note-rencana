/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface CategoriesControllerFindAll$Params {

/**
 * Filter by owner ID
 */
  ownerId?: string;

/**
 * Filter by industry code
 */
  industryCode?: string;
  isOperational?: boolean;

/**
 * Include system categories (default: true)
 */
  includeSystem?: boolean;
}

export function categoriesControllerFindAll(http: HttpClient, rootUrl: string, params?: CategoriesControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, categoriesControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('ownerId', params.ownerId, {});
    rb.query('industryCode', params.industryCode, {});
    rb.query('isOperational', params.isOperational, {});
    rb.query('includeSystem', params.includeSystem, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

categoriesControllerFindAll.PATH = '/api/webgis/categories';
