/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FeatureResponseDto } from '../../models/feature-response-dto';
import { UpdateFeatureDto } from '../../models/update-feature-dto';

export interface FeaturesControllerUpdate$Params {
  id: string;
      body: UpdateFeatureDto
}

export function featuresControllerUpdate(http: HttpClient, rootUrl: string, params: FeaturesControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<FeatureResponseDto>> {
  const rb = new RequestBuilder(rootUrl, featuresControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FeatureResponseDto>;
    })
  );
}

featuresControllerUpdate.PATH = '/api/webgis/features/{id}';
