/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AnomaliesControllerFindAll$Params {

/**
 * Filter by sensor channel ID
 */
  idSensorChannel?: string;

/**
 * Filter by device ID
 */
  deviceId?: string;

/**
 * Filter by sensor key
 */
  sensorKey?: string;

/**
 * Filter by owner ID
 */
  ownerId?: string;

/**
 * Filter by minimum grade
 */
  minGrade?: 'normal' | 'mild' | 'moderate' | 'severe' | 'critical';

/**
 * Start date for date range filter
 */
  startDate?: string;

/**
 * End date for date range filter
 */
  endDate?: string;

/**
 * Filter only acknowledged anomalies
 */
  isAcknowledged?: boolean;
  page?: number;
  limit?: number;
}

export function anomaliesControllerFindAll(http: HttpClient, rootUrl: string, params?: AnomaliesControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, anomaliesControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('idSensorChannel', params.idSensorChannel, {});
    rb.query('deviceId', params.deviceId, {});
    rb.query('sensorKey', params.sensorKey, {});
    rb.query('ownerId', params.ownerId, {});
    rb.query('minGrade', params.minGrade, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('isAcknowledged', params.isAcknowledged, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

anomaliesControllerFindAll.PATH = '/api/ml/anomalies';
