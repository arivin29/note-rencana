/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnomalySummaryDto } from '../../models/anomaly-summary-dto';

export interface AnomaliesControllerGetSummary$Params {

/**
 * Filter by owner ID
 */
  ownerId?: string;

/**
 * Start date filter (ISO 8601)
 */
  startDate?: string;

/**
 * End date filter (ISO 8601)
 */
  endDate?: string;
}

export function anomaliesControllerGetSummary(http: HttpClient, rootUrl: string, params?: AnomaliesControllerGetSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<AnomalySummaryDto>> {
  const rb = new RequestBuilder(rootUrl, anomaliesControllerGetSummary.PATH, 'get');
  if (params) {
    rb.query('ownerId', params.ownerId, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AnomalySummaryDto>;
    })
  );
}

anomaliesControllerGetSummary.PATH = '/api/ml/anomalies/summary';
