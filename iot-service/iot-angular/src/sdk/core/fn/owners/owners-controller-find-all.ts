/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedResponseDto } from '../../models/paginated-response-dto';

export interface OwnersControllerFindAll$Params {

/**
 * Page number
 */
  page?: number;

/**
 * Items per page
 */
  limit?: number;

/**
 * Search keyword
 */
  search?: string;

/**
 * Field to sort by
 */
  sortBy?: string;

/**
 * Sort order
 */
  sortOrder?: 'ASC' | 'DESC';

/**
 * Filter by industry
 */
  industry?: string;

/**
 * Filter by multiple industries (comma-separated)
 */
  industries?: string;

/**
 * Filter by SLA level
 */
  slaLevel?: string;

/**
 * Filter by multiple SLA levels (comma-separated)
 */
  slaLevels?: string;

/**
 * Filter by contact person name
 */
  contactPerson?: string;

/**
 * Filter by created date from (ISO format)
 */
  createdFrom?: string;

/**
 * Filter by created date to (ISO format)
 */
  createdTo?: string;

/**
 * Filter by updated date from (ISO format)
 */
  updatedFrom?: string;

/**
 * Filter by updated date to (ISO format)
 */
  updatedTo?: string;

/**
 * Filter by project IDs (comma-separated)
 */
  projectIds?: string;

/**
 * Filter by project name (partial match)
 */
  projectName?: string;

/**
 * Filter by project status
 */
  projectStatus?: string;

/**
 * Filter owners that have nodes
 */
  hasNodes?: string;

/**
 * Filter owners that have active sensors
 */
  hasActiveSensors?: string;

/**
 * Minimum number of projects
 */
  minProjects?: string;

/**
 * Maximum number of projects
 */
  maxProjects?: string;
}

export function ownersControllerFindAll(http: HttpClient, rootUrl: string, params?: OwnersControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ownersControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('search', params.search, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('sortOrder', params.sortOrder, {});
    rb.query('industry', params.industry, {});
    rb.query('industries', params.industries, {});
    rb.query('slaLevel', params.slaLevel, {});
    rb.query('slaLevels', params.slaLevels, {});
    rb.query('contactPerson', params.contactPerson, {});
    rb.query('createdFrom', params.createdFrom, {});
    rb.query('createdTo', params.createdTo, {});
    rb.query('updatedFrom', params.updatedFrom, {});
    rb.query('updatedTo', params.updatedTo, {});
    rb.query('projectIds', params.projectIds, {});
    rb.query('projectName', params.projectName, {});
    rb.query('projectStatus', params.projectStatus, {});
    rb.query('hasNodes', params.hasNodes, {});
    rb.query('hasActiveSensors', params.hasActiveSensors, {});
    rb.query('minProjects', params.minProjects, {});
    rb.query('maxProjects', params.maxProjects, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedResponseDto>;
    })
  );
}

ownersControllerFindAll.PATH = '/api/owners';
