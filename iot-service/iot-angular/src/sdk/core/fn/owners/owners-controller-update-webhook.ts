/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ForwardingWebhookResponseDto } from '../../models/forwarding-webhook-response-dto';
import { UpdateWebhookDto } from '../../models/update-webhook-dto';

export interface OwnersControllerUpdateWebhook$Params {

/**
 * Owner ID
 */
  id: string;

/**
 * Webhook ID
 */
  webhookId: string;
      body: UpdateWebhookDto
}

export function ownersControllerUpdateWebhook(http: HttpClient, rootUrl: string, params: OwnersControllerUpdateWebhook$Params, context?: HttpContext): Observable<StrictHttpResponse<ForwardingWebhookResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ownersControllerUpdateWebhook.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('webhookId', params.webhookId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ForwardingWebhookResponseDto>;
    })
  );
}

ownersControllerUpdateWebhook.PATH = '/api/owners/{id}/webhooks/{webhookId}';
