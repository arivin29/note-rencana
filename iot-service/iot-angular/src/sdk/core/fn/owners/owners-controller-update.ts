/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OwnerResponseDto } from '../../models/owner-response-dto';
import { UpdateOwnerDto } from '../../models/update-owner-dto';

export interface OwnersControllerUpdate$Params {

/**
 * Owner UUID
 */
  id: string;
      body: UpdateOwnerDto
}

export function ownersControllerUpdate(http: HttpClient, rootUrl: string, params: OwnersControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<OwnerResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ownersControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OwnerResponseDto>;
    })
  );
}

ownersControllerUpdate.PATH = '/api/owners/{id}';
