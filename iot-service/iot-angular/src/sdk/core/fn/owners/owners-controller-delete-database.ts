/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface OwnersControllerDeleteDatabase$Params {

/**
 * Owner ID
 */
  id: string;

/**
 * Database config ID
 */
  databaseId: string;
}

export function ownersControllerDeleteDatabase(http: HttpClient, rootUrl: string, params: OwnersControllerDeleteDatabase$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, ownersControllerDeleteDatabase.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('databaseId', params.databaseId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

ownersControllerDeleteDatabase.PATH = '/api/owners/{id}/databases/{databaseId}';
