/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ForwardingDatabaseResponseDto } from '../../models/forwarding-database-response-dto';
import { UpdateDatabaseDto } from '../../models/update-database-dto';

export interface OwnersControllerUpdateDatabase$Params {

/**
 * Owner ID
 */
  id: string;

/**
 * Database config ID
 */
  databaseId: string;
      body: UpdateDatabaseDto
}

export function ownersControllerUpdateDatabase(http: HttpClient, rootUrl: string, params: OwnersControllerUpdateDatabase$Params, context?: HttpContext): Observable<StrictHttpResponse<ForwardingDatabaseResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ownersControllerUpdateDatabase.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('databaseId', params.databaseId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ForwardingDatabaseResponseDto>;
    })
  );
}

ownersControllerUpdateDatabase.PATH = '/api/owners/{id}/databases/{databaseId}';
