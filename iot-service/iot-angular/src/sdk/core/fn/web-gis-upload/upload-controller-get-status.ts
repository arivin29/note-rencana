/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UploadStatusResponseDto } from '../../models/upload-status-response-dto';

export interface UploadControllerGetStatus$Params {
  id: string;
}

export function uploadControllerGetStatus(http: HttpClient, rootUrl: string, params: UploadControllerGetStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<UploadStatusResponseDto>> {
  const rb = new RequestBuilder(rootUrl, uploadControllerGetStatus.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UploadStatusResponseDto>;
    })
  );
}

uploadControllerGetStatus.PATH = '/api/webgis/upload/{id}/status';
