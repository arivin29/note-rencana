/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IotLogStatsDto } from '../../models/iot-log-stats-dto';

export interface IotLogsControllerGetStats$Params {

/**
 * Filter by device ID (node code)
 */
  deviceId?: string;

/**
 * Filter by owner ID (UUID)
 */
  ownerId?: string;

/**
 * Filter by project ID (UUID)
 */
  projectId?: string;

/**
 * Filter by log label/category
 */
  label?: 'telemetry' | 'event' | 'pairing' | 'error' | 'warning' | 'command' | 'response' | 'debug' | 'info' | 'log';

/**
 * Filter by processed status
 */
  processed?: boolean;

/**
 * Start date for filtering (ISO 8601 format)
 */
  startDate?: string;

/**
 * End date for filtering (ISO 8601 format)
 */
  endDate?: string;

/**
 * Page number for pagination
 */
  page?: number;

/**
 * Number of items per page
 */
  limit?: number;
}

export function iotLogsControllerGetStats(http: HttpClient, rootUrl: string, params?: IotLogsControllerGetStats$Params, context?: HttpContext): Observable<StrictHttpResponse<IotLogStatsDto>> {
  const rb = new RequestBuilder(rootUrl, iotLogsControllerGetStats.PATH, 'get');
  if (params) {
    rb.query('deviceId', params.deviceId, {});
    rb.query('ownerId', params.ownerId, {});
    rb.query('projectId', params.projectId, {});
    rb.query('label', params.label, {});
    rb.query('processed', params.processed, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IotLogStatsDto>;
    })
  );
}

iotLogsControllerGetStats.PATH = '/api/iot-logs/stats';
