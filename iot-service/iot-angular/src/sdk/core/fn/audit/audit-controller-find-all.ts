/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AuditControllerFindAll$Params {

/**
 * Filter by user ID
 */
  idUser?: string;

/**
 * Filter by action type
 */
  action?: 'create' | 'read' | 'update' | 'delete' | 'login' | 'logout' | 'password_change' | 'status_change';

/**
 * Filter by entity type (e.g., User, Owner, Node)
 */
  entityType?: string;

/**
 * Filter by entity ID
 */
  entityId?: string;

/**
 * Filter by status
 */
  status?: 'success' | 'failure';

/**
 * Filter by start date (ISO 8601 format)
 */
  startDate?: string;

/**
 * Filter by end date (ISO 8601 format)
 */
  endDate?: string;

/**
 * Search in description, IP, user agent
 */
  search?: string;

/**
 * Page number
 */
  page?: number;

/**
 * Number of items per page
 */
  limit?: number;
}

export function auditControllerFindAll(http: HttpClient, rootUrl: string, params?: AuditControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, auditControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('idUser', params.idUser, {});
    rb.query('action', params.action, {});
    rb.query('entityType', params.entityType, {});
    rb.query('entityId', params.entityId, {});
    rb.query('status', params.status, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('search', params.search, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

auditControllerFindAll.PATH = '/api/audit';
