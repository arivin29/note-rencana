/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LayerListResponseDto } from '../../models/layer-list-response-dto';

export interface LayersControllerFindAll$Params {

/**
 * Filter by project ID
 */
  projectId?: string;

/**
 * Filter by owner ID
 */
  ownerId?: string;
  layerType?: 'core' | 'operational' | 'custom';
  categoryCode?: string;
  search?: string;
  page?: number;
  limit?: number;
}

export function layersControllerFindAll(http: HttpClient, rootUrl: string, params?: LayersControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<LayerListResponseDto>> {
  const rb = new RequestBuilder(rootUrl, layersControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('projectId', params.projectId, {});
    rb.query('ownerId', params.ownerId, {});
    rb.query('layerType', params.layerType, {});
    rb.query('categoryCode', params.categoryCode, {});
    rb.query('search', params.search, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LayerListResponseDto>;
    })
  );
}

layersControllerFindAll.PATH = '/api/webgis/layers';
