/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GeoJsonResponseDto } from '../../models/geo-json-response-dto';

export interface LayersControllerGetGeoJson$Params {
  id: string;

/**
 * Bounding box: minX,minY,maxX,maxY
 */
  bbox?: string;
}

export function layersControllerGetGeoJson(http: HttpClient, rootUrl: string, params: LayersControllerGetGeoJson$Params, context?: HttpContext): Observable<StrictHttpResponse<GeoJsonResponseDto>> {
  const rb = new RequestBuilder(rootUrl, layersControllerGetGeoJson.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('bbox', params.bbox, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GeoJsonResponseDto>;
    })
  );
}

layersControllerGetGeoJson.PATH = '/api/webgis/layers/{id}/geojson';
