/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateLayerDto } from '../../models/create-layer-dto';
import { LayerResponseDto } from '../../models/layer-response-dto';

export interface LayersControllerCreate$Params {
      body: CreateLayerDto
}

export function layersControllerCreate(http: HttpClient, rootUrl: string, params: LayersControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<LayerResponseDto>> {
  const rb = new RequestBuilder(rootUrl, layersControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LayerResponseDto>;
    })
  );
}

layersControllerCreate.PATH = '/api/webgis/layers';
