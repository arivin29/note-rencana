/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateUnpairedDeviceDto } from '../../models/create-unpaired-device-dto';
import { UnpairedDeviceResponseDto } from '../../models/unpaired-device-response-dto';

export interface UnpairedDevicesControllerCreate$Params {
      body: CreateUnpairedDeviceDto
}

export function unpairedDevicesControllerCreate(http: HttpClient, rootUrl: string, params: UnpairedDevicesControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<UnpairedDeviceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, unpairedDevicesControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UnpairedDeviceResponseDto>;
    })
  );
}

unpairedDevicesControllerCreate.PATH = '/api/unpaired-devices';
