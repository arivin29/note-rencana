/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UnpairedDeviceResponseDto } from '../../models/unpaired-device-response-dto';
import { UpdateUnpairedDeviceDto } from '../../models/update-unpaired-device-dto';

export interface UnpairedDevicesControllerUpdate$Params {

/**
 * Unpaired device UUID
 */
  id: string;
      body: UpdateUnpairedDeviceDto
}

export function unpairedDevicesControllerUpdate(http: HttpClient, rootUrl: string, params: UnpairedDevicesControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<UnpairedDeviceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, unpairedDevicesControllerUpdate.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UnpairedDeviceResponseDto>;
    })
  );
}

unpairedDevicesControllerUpdate.PATH = '/api/unpaired-devices/{id}';
