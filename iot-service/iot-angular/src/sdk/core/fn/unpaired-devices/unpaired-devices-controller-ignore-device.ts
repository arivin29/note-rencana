/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UnpairedDeviceResponseDto } from '../../models/unpaired-device-response-dto';

export interface UnpairedDevicesControllerIgnoreDevice$Params {

/**
 * Unpaired device UUID
 */
  id: string;
}

export function unpairedDevicesControllerIgnoreDevice(http: HttpClient, rootUrl: string, params: UnpairedDevicesControllerIgnoreDevice$Params, context?: HttpContext): Observable<StrictHttpResponse<UnpairedDeviceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, unpairedDevicesControllerIgnoreDevice.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UnpairedDeviceResponseDto>;
    })
  );
}

unpairedDevicesControllerIgnoreDevice.PATH = '/api/unpaired-devices/{id}/ignore';
