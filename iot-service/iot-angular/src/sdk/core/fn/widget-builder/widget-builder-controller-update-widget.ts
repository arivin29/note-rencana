/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CustomWidgetResponseDto } from '../../models/custom-widget-response-dto';
import { UpdateWidgetDto } from '../../models/update-widget-dto';

export interface WidgetBuilderControllerUpdateWidget$Params {

/**
 * Dashboard ID
 */
  dashboardId: string;

/**
 * Widget ID
 */
  widgetId: string;
      body: UpdateWidgetDto
}

export function widgetBuilderControllerUpdateWidget(http: HttpClient, rootUrl: string, params: WidgetBuilderControllerUpdateWidget$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomWidgetResponseDto>> {
  const rb = new RequestBuilder(rootUrl, widgetBuilderControllerUpdateWidget.PATH, 'put');
  if (params) {
    rb.path('dashboardId', params.dashboardId, {});
    rb.path('widgetId', params.widgetId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomWidgetResponseDto>;
    })
  );
}

widgetBuilderControllerUpdateWidget.PATH = '/api/widget-builder/dashboards/{dashboardId}/widgets/{widgetId}';
