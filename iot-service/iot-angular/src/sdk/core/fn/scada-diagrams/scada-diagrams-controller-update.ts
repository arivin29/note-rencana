/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScadaDiagramDetailResponseDto } from '../../models/scada-diagram-detail-response-dto';
import { UpdateScadaDiagramDto } from '../../models/update-scada-diagram-dto';

export interface ScadaDiagramsControllerUpdate$Params {
  diagramId: string;
      body: UpdateScadaDiagramDto
}

export function scadaDiagramsControllerUpdate(http: HttpClient, rootUrl: string, params: ScadaDiagramsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<ScadaDiagramDetailResponseDto>> {
  const rb = new RequestBuilder(rootUrl, scadaDiagramsControllerUpdate.PATH, 'put');
  if (params) {
    rb.path('diagramId', params.diagramId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScadaDiagramDetailResponseDto>;
    })
  );
}

scadaDiagramsControllerUpdate.PATH = '/api/scada/diagrams/{diagramId}';
