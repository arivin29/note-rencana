/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NodeModelResponseDto } from '../../models/node-model-response-dto';
import { UpdateNodeModelDto } from '../../models/update-node-model-dto';

export interface NodeModelsControllerUpdate$Params {
  id: string;
      body: UpdateNodeModelDto
}

export function nodeModelsControllerUpdate(http: HttpClient, rootUrl: string, params: NodeModelsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<NodeModelResponseDto>> {
  const rb = new RequestBuilder(rootUrl, nodeModelsControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NodeModelResponseDto>;
    })
  );
}

nodeModelsControllerUpdate.PATH = '/api/node-models/{id}';
