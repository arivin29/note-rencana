/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeliveryHealthResponseDto } from '../../models/delivery-health-response-dto';

export interface DashboardControllerGetDeliveryHealth$Params {

/**
 * Filter by specific owner ID
 */
  ownerId?: string;

/**
 * Filter by specific project ID
 */
  projectId?: string;

/**
 * Time range for data aggregation
 */
  timeRange?: '24h' | '7d' | '30d';

/**
 * Limit number of results
 */
  limit?: number;
}

export function dashboardControllerGetDeliveryHealth(http: HttpClient, rootUrl: string, params?: DashboardControllerGetDeliveryHealth$Params, context?: HttpContext): Observable<StrictHttpResponse<DeliveryHealthResponseDto>> {
  const rb = new RequestBuilder(rootUrl, dashboardControllerGetDeliveryHealth.PATH, 'get');
  if (params) {
    rb.query('ownerId', params.ownerId, {});
    rb.query('projectId', params.projectId, {});
    rb.query('timeRange', params.timeRange, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeliveryHealthResponseDto>;
    })
  );
}

dashboardControllerGetDeliveryHealth.PATH = '/api/dashboard/delivery-health';
