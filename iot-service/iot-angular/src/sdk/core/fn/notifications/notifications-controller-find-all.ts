/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface NotificationsControllerFindAll$Params {

/**
 * Filter by user ID
 */
  idUser?: string;

/**
 * Filter by notification type
 */
  type?: 'info' | 'warning' | 'error' | 'success' | 'alert';

/**
 * Filter by notification status
 */
  status?: 'pending' | 'sent' | 'failed' | 'read';

/**
 * Filter by read status
 */
  isRead?: boolean;

/**
 * Search in title and message
 */
  search?: string;

/**
 * Page number
 */
  page?: number;

/**
 * Number of items per page
 */
  limit?: number;
}

export function notificationsControllerFindAll(http: HttpClient, rootUrl: string, params?: NotificationsControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, notificationsControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('idUser', params.idUser, {});
    rb.query('type', params.type, {});
    rb.query('status', params.status, {});
    rb.query('isRead', params.isRead, {});
    rb.query('search', params.search, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

notificationsControllerFindAll.PATH = '/api/notifications';
