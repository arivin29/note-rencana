/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ForecastsControllerGetForecastRange$Params {
  idSensorChannel: string;

/**
 * Start time (ISO 8601)
 */
  startTime: string;

/**
 * End time (ISO 8601)
 */
  endTime: string;
}

export function forecastsControllerGetForecastRange(http: HttpClient, rootUrl: string, params: ForecastsControllerGetForecastRange$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, forecastsControllerGetForecastRange.PATH, 'get');
  if (params) {
    rb.path('idSensorChannel', params.idSensorChannel, {});
    rb.query('startTime', params.startTime, {});
    rb.query('endTime', params.endTime, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

forecastsControllerGetForecastRange.PATH = '/api/ml/forecasts/range/{idSensorChannel}';
