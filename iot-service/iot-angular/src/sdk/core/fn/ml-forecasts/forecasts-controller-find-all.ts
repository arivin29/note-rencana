/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ForecastsControllerFindAll$Params {

/**
 * Filter by sensor channel ID
 */
  idSensorChannel?: string;

/**
 * Filter by device ID
 */
  deviceId?: string;

/**
 * Filter by sensor key
 */
  sensorKey?: string;

/**
 * Filter by owner ID
 */
  ownerId?: string;

/**
 * Forecast start time (defaults to now)
 */
  startTime?: string;

/**
 * Forecast end time (defaults to 7 days from now)
 */
  endTime?: string;
  page?: number;

/**
 * Number of forecast points
 */
  limit?: number;
}

export function forecastsControllerFindAll(http: HttpClient, rootUrl: string, params?: ForecastsControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, forecastsControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('idSensorChannel', params.idSensorChannel, {});
    rb.query('deviceId', params.deviceId, {});
    rb.query('sensorKey', params.sensorKey, {});
    rb.query('ownerId', params.ownerId, {});
    rb.query('startTime', params.startTime, {});
    rb.query('endTime', params.endTime, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

forecastsControllerFindAll.PATH = '/api/ml/forecasts';
