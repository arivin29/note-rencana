/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateSensorChannelDto } from '../../models/create-sensor-channel-dto';
import { SensorChannelResponseDto } from '../../models/sensor-channel-response-dto';

export interface SensorChannelsControllerCreate$Params {
      body: CreateSensorChannelDto
}

export function sensorChannelsControllerCreate(http: HttpClient, rootUrl: string, params: SensorChannelsControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<SensorChannelResponseDto>> {
  const rb = new RequestBuilder(rootUrl, sensorChannelsControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SensorChannelResponseDto>;
    })
  );
}

sensorChannelsControllerCreate.PATH = '/api/sensor-channels';
