import { Component } from '@angular/core';

/** Shell Detail Node: tab-bar (router) Info / Command / Logs + outlet. */
@Component({
  selector: 'mobile-node-detail',
  templateUrl: './mobile-node-detail.component.html',
  standalone: false
})
export class MobileNodeDetailComponent {}
