import { Component } from '@angular/core';

/** Shell project detail: tab-bar (router) + outlet. Tiap tab = child route. docs/mobile/05 §8. */
@Component({
  selector: 'mobile-project-detail',
  templateUrl: './mobile-project-detail.component.html',
  standalone: false
})
export class MobileProjectDetailComponent {}
